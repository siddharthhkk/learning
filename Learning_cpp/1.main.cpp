#include <iostream>
using namespace std;
int main(){ 
    cout << "Hi i am Siddharth!\n";
    /* DataTypes-
    int (whole number)
    double (number including decimal) float is only accurate till 7 decimal while double goes till 15.
    char (a single character)
    bool (boolean values only outputs true or false) bool student = true;
    string (object that represents a sequence of text or array of characters)
    */ 
    /* const keyword is used to make a variable constant and prevents the compiler from modifying it.(you cannot change the value in the code)
     const double PI = 3.14159; it is generally a good practice to use capital letters for a const variable.
    */
    return 0;
}